Project : first One Hook Learn Here 'useState'. 
-> create Elements H1,H2 and button(increment and decrement).
-> create useState Hook, syntax: 'datatype [variable, method] = useState()'. enter variable name counter and method setCounter.
-> Added onClick in Button with function name of incressValue and decressValue.
-> create function for handle buttons (incressValue and decressValue).
-> write logic into function.